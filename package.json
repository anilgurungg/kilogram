{
  "name": "kilogram",
  "version": "1.0.0",
  "description": "clone of instagram",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm rum build ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "anil gurung",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "custom-env": "^2.0.1",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.10.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
